#include <iostream>
using namespace std;

int main() {
    int* ip = nullptr;      // ip is a pointer variable initialized to nullptr (null pointer)
    int*& r = ip;           // r is a reference to pointer ip; r and ip refer to the same pointer

    int i;                  // i is an int type variable
    int* ip2 = nullptr;     // ip2 is a pointer to int, initialized to nullptr

    int* ip3;               // ip3 is a pointer to int (uninitialized)
    int ip4;                // ip4 is an int variable

   
}
