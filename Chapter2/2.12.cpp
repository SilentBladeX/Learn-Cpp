 // (a) syntax error bcz the double is keyword in Cpp
  int double = 3.14;

 // (b) there's no error we can also use underscore(_) as or between variable name
  int _;

// (c) Error bcz we cannot use hyphens in variable name
  int catch-22;

 //(d) Error bcz variable name connot start with constant values
  int 1_or_2 = 1;

 //(e) No error bcz it is case sensitive language so keyword in cpp is double and we use Double
  double Double = 3.14;